import 'package:flutter/material.dart';

class Bloodbank_login_screen extends StatelessWidget {
  const Bloodbank_login_screen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Bloodbank Login"),
    );
  }
}