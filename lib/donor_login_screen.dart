import 'package:flutter/material.dart';

class Donor_login_screen extends StatelessWidget {
  const Donor_login_screen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Donor Login"),
    );
  }
}