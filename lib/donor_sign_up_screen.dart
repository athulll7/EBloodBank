import 'package:flutter/material.dart';

class Donor_sign_up_screen extends StatelessWidget {
  const Donor_sign_up_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Donor Sign Up Page"),
    );
  }
}
